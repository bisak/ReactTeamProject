const components = require('./components-controller')
const users = require('./users-controller')
const admins = require('./admins-controller')

module.exports = {
  components: components,
  users: users,
  admins: admins
}
